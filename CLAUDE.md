# プロジェクト固有ルール

## 実装ルール

### 0. 不明点の事前確認（必須・厳守）
- 作業開始前に要件・仕様・デザイン等に不明点がある場合は**必ずユーザーに質問**する
- **一切不明点がない状態**になってから作業を開始する
- 「以前の取り決め」と矛盾する指示があった場合も必ず確認する

### 0-2. 問題・修正点の管理プロセス（必須・厳守）
ユーザーから修正点・問題点が共有された場合、以下のプロセスを**絶対に**守る：

```
修正点・問題点の共有 → problem.txt に記録
→ problem.txt の内容をもとにタスクを切り出し progress.txt に追加
→ 修正作業の実施 → 動作確認 → problem.txt / progress.txt を更新
```

- ⛔ problem.txt への記録なしに修正作業を開始してはならない
- ⛔ progress.txt へのタスク追加なしに修正作業を開始してはならない
- バグに限らず、デザイン変更・機能変更などの修正依頼もすべてこのプロセスの対象とする

### 1. スコープ厳守
- 指示されたタスク（Phase/Work）以上のことは絶対に実行しない
- 指示されたPhaseが完了したら必ず作業を停止する
- 完了後は何をどのように実装したかを報告する

### 1-2. フェーズ単位の停止・確認フロー（必須・厳守）
各フェーズが終了したら、**必ず**以下のフローを守る：

```
フェーズ完了 → 作業を停止 → ユーザーが確認 → 修正点を共有
→ 修正を実施 → 修正完了をユーザーが確認 → 次のフェーズへ進む
```

- ⛔ **1つのフェーズが完了したら、ユーザーの承認なしに次のフェーズへ自動的に進んではならない**
- フェーズ完了時は必ず作業を停止し、ユーザーの確認を待つ
- 修正点が共有された場合は、修正が完了しユーザーが確認するまで次に進まない
- 「次のフェーズに進んでよいか」を明示的に確認してから進む

### 2. 報告形式
Phase完了時に以下を報告：
- 実装したファイル一覧
- 各ファイルの概要
- 動作確認方法
- 次のPhaseに進む準備ができているか

### 3. 進捗管理
- タスク完了時は progress.txt を更新
- 完了タスクは progress-archive.txt に移動

### 4. 問題発生時のワークフロー（必須）
ユーザーからアプリの問題・バグが報告された場合、以下のフローを**必ず**実行する：

```
問題報告 → 原因調査 → 修正計画立案 → problem.txtに記録 → 修正実施 → 動作確認 → problem.txt更新
```

**problem.txt への記録内容:**
1. 発見日
2. 症状（ユーザーが報告した現象）
3. 影響範囲（どのファイル・機能に影響するか）
4. 原因（技術的な根本原因）
5. 修正計画（Phase分け）
6. ステータス（未着手/進行中/完了）

**注意事項:**
- 原因調査は徹底的に行い、表面的な対処ではなく根本原因を特定する
- 修正前に必ず problem.txt に記録してからコードを変更する
- 修正完了後は problem.txt のステータスを更新する

## フォントルール（必須・全ページ共通）
このサイトのフォントは **portfolio-2025 サイトと全く同じ**スタックに統一する。
今後のページ追加・修正でも必ずこのフォントを徹底すること。

- 定義場所: `src/styles/variables.css` の `--font-family-base` / `--font-family-mono`
- **本文/全体（明朝体スタック）**: `游明朝, YuMincho, 'ヒラギノ明朝 ProN W3', 'Hiragino Mincho ProN', HG明朝E, 'ＭＳ Ｐ明朝', 'ＭＳ 明朝', serif`
- **英字表記用（欧文サンセリフ）**: `'Helvetica Neue', Helvetica, Arial, sans-serif`
  - ※ 変数名は歴史的経緯で `--font-family-mono` のままだが、役割は「英字用サンセリフ」

**禁止事項:**
- ⛔ コンポーネント/ページで `font-family` を直接ハードコードしない
- ⛔ 必ず `var(--font-family-base)` / `var(--font-family-mono)` を参照する
- ⛔ Webフォント（Google Fonts等）の読み込み追加も禁止（portfolio-2025 と同じシステムフォント方式を維持）
- 例外: `src/components/pdf/SkillSheetTemplate.css`（PDF出力書類の字体）はサイトUIと別管理

## デザイン要素ルール（必須）
- ⛔ 装飾目的の連番（01, 02, 03 のようなセクション番号）を要素に入れない。
  ユーザーからの明示的な指示がある場合のみ追加してよい。
  （2026-08-22 ユーザー指示により恒久ルール化）

## 技術スタック
- React（初学者向けコード）
- JavaScript
- GitHub Pages でホスティング

## UI開発
- frontend-design スキルを使用すること
